import 'package:flutter/cupertino.dart';

class Fallback extends StatelessWidget {
  const Fallback({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("fall back page"),);
  }
}
